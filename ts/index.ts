const number: number = 0x14;
const string: string = 'jack';
const array: Array<number> = [1, 2, 3];
const arr: number[] = [1, 2, 3];

const turkle: [string, number] = ['1', 2];
turkle[3] = 'dj';
// console.log(a);

enum Color {Red = 1, Green, Blue}

const colorname: string = Color[2];
const colorName: number = Color.Red;
