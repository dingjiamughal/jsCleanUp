<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title news
    body
        ul
            each n in news
                li
                    span #{n.title}
                    span #{n.url}
                    span #{helper.relative(n.time)}